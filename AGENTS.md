# Cashier POS web app

This project is a Vite-powered React web application. Use `npm run dev` for local development,
`npm run build` for a production build, and keep the browser compatibility layer based on
`react-native-web` until the shared POS screens are intentionally redesigned.
